// 材料ロット一覧、手動在庫調整をまとめる予定のページ。
// 対応するバックエンドAPI: /api/material-lots, /api/material-lots/{id}/adjustments
export default function InventoryPage() {
  return (
    <div className="container py-4">
      <h1 className="h3 mb-4">在庫</h1>
      <p className="text-muted">この画面は準備中です。</p>
    </div>
  );
}

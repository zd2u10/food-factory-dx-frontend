// Draft作成→FEFO確認→実行→検品完了、MRP実行をまとめる予定のページ。
// 対応するバックエンドAPI: /api/items/{id}/batches, /api/batches/*, /api/mrp/run
export default function ManufacturingPage() {
  return (
    <div className="container py-4">
      <h1 className="h3 mb-4">製造</h1>
      <p className="text-muted">この画面は準備中です。</p>
    </div>
  );
}
